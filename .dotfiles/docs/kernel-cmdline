Ensure, that /etc/kernel/cmdline exists. Else cmdline.d is ignored
Kernel commandline parameter in /etc/cmdline.d/[file].conf

1. clocksource.conf:
tsc=unstable

luks setup in /etc/kernel/cmdline or:
2. root.conf:
root=UUID=<UUID> rw rootargs=subvol=root

